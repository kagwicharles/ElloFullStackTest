class Book {
    constructor(title, coverPhotoURL, author, isAssigned) {
      this.title = title;
      this.coverPhotoURL = coverPhotoURL;
      this.author = author;
      this.isAssigned = isAssigned;
    }
  }
  
  export default Book;
  